package documents;

public class WordDocument extends Document {
    public WordDocument(int id, String type, Long printingDuration, PrintPaper paperSize) {
        super(id, type, printingDuration, paperSize);
    }
}
